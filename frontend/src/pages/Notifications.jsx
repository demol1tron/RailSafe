import {useQuery,useQueryClient} from '@tanstack/react-query';import {Link} from 'react-router-dom';import {api} from '../api/client';import {translateKnownEnumsInText} from '../utils/labels';export default function Notifications(){const qc=useQueryClient();const q=useQuery({queryKey:['notifications'],queryFn:()=>api.get('/notifications').then(r=>r.data)});const read=async(id)=>{await api.patch(`/notifications/${id}/read`);qc.invalidateQueries({queryKey:['notifications']})};const all=async()=>{await api.patch('/notifications/read-all');qc.invalidateQueries({queryKey:['notifications']})};return <><div className="page-head"><div><h1>Уведомления</h1><p>Назначения и изменения статусов</p></div><button className="secondary" onClick={all}>Отметить все прочитанными</button></div><div className="panel notifications">{q.data?.map(n=><div key={n.id} className={n.is_read?'read':''}><span className="notif-dot"></span><div><b>{translateKnownEnumsInText(n.message)}</b><small>{new Date(n.created_at).toLocaleString('ru-RU')}</small>{n.incident_id&&<Link to={`/incidents/${n.incident_id}`}>Открыть инцидент</Link>}</div>{!n.is_read&&<button className="linkbtn" onClick={()=>read(n.id)}>Прочитано</button>}</div>)}{!q.data?.length&&<p className="muted">Уведомлений нет.</p>}</div></>}
