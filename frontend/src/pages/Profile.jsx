import {useState} from 'react';import {api,errorText} from '../api/client';import {useAuth} from '../contexts/AuthContext';import {roleLabel} from '../utils/labels';export default function Profile(){const {user,setUser}=useAuth();const [form,setForm]=useState({full_name:user.full_name,phone:user.phone||''});const [pass,setPass]=useState({old_password:'',new_password:''});const [message,setMessage]=useState('');const save=async(e)=>{e.preventDefault();try{const r=await api.put('/profile',{...form,phone:form.phone||null});setUser(r.data);setMessage('Профиль сохранён')}catch(err){setMessage(errorText(err))}};const change=async(e)=>{e.preventDefault();try{await api.post('/profile/password',pass);setPass({old_password:'',new_password:''});setMessage('Пароль изменён. Активные refresh-токены отозваны.')}catch(err){setMessage(errorText(err))}};return <><div className="page-head"><div><h1>Профиль</h1><p>{user.email} · {roleLabel(user.role)}</p></div></div>{message&&<div className="notice">{message}</div>}<div className="grid two"><form className="panel" onSubmit={save}><h2>Личные данные</h2><label>ФИО<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Телефон<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><button>Сохранить</button></form><form className="panel" onSubmit={change}><h2>Смена пароля</h2><label>Текущий пароль<input type="password" value={pass.old_password} onChange={e=>setPass({...pass,old_password:e.target.value})}/></label><label>Новый пароль<input type="password" minLength="8" value={pass.new_password} onChange={e=>setPass({...pass,new_password:e.target.value})}/></label><button>Изменить пароль</button></form></div></>}
