import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';import {errorText} from '../api/client';
export default function Register(){const {register}=useAuth();const nav=useNavigate();const [form,setForm]=useState({email:'',password:'',repeat:'',full_name:'',phone:''});const [error,setError]=useState('');const submit=async(e)=>{e.preventDefault();if(form.password!==form.repeat){setError('Пароли не совпадают');return}try{await register({email:form.email,password:form.password,full_name:form.full_name,phone:form.phone||null});nav('/login')}catch(err){setError(errorText(err))}};return <div className="auth-page"><div className="auth-card wide"><h1>Регистрация</h1><p>Роль назначается автоматически: Сотрудник. Диспетчеров и инспекторов создаёт администратор.</p><form onSubmit={submit}><label>ФИО<input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Телефон<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Пароль<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Повторите пароль<input type="password" required value={form.repeat} onChange={e=>setForm({...form,repeat:e.target.value})}/></label>{error&&<div className="alert error">{error}</div>}<button>Создать аккаунт</button></form><Link to="/login">Вернуться ко входу</Link></div></div>}
